# التوثيق الزمني · Changelog

كل نسخة موثَّقة بوسم في git، وكل سطر هنا مكتوب من الـcommits ومن مخرجات `npm test`،
لا من ذاكرة من كتبها. الصيغة على نَسَق Keep a Changelog، والتواريخ هي تواريخ الوسوم.

## [Unreleased]

### رابط لوحة الإدارة في التذييل

- **الوصولُ للوحة بلا حفظ مسار**: عمود «الشركة» في تذييل المتجر يحمل الآن رابط
  «لوحة الإدارة» / “Admin panel” إلى `/admin` (`footer.admin` في اللغتين، والرابطُ
  في `src/components/Footer.jsx`) — وهو ما كان مكتوبًا في README منذ v1.9.0 بلا
  رابطٍ في الكود فعلًا، فصار التوثيقُ مطابقًا لما يُعرض.
- **قرارٌ معلن لا إخفاء**: الرابطُ ظاهرٌ لكل زائر، والحمايةُ تبقى كلمةَ السرّ
  (`adminLocal.js` في وضع الجهاز، وscrypt + كوكي HttpOnly في `server/admin.js`).
  والصفحة تظل `noindex,follow` وممنوعة في `robots.txt` وغائبةً عن `sitemap.xml`.
- **فحصان حُدِّثا مع القرار**: `the footer carries the way into the staff panel`
  (بدل الفحص الذي كان يمنع الرابط عمدًا) و`the admin path is linked on purpose yet
kept away from crawlers` الذي يثبّت في الوقت نفسه أن الرابط موجود وأن `/admin`
  خارج الفهرسة — فحذفُ الرابط سهوًا يُسقط الفحص.

### إصلاح حزم التسليم واستوديو تخصيص المشتري

- **إصلاح — الحزمة تُفتح مصمَّمة من القرص مباشرة**: كل مسار في ملفّات التسليم صار نسبيًّا
  نظيفًا (`./styles.css` · `./assets/content.js` · `./projects/…` · `./content/profile.json` في
  الـ fetch)، وصفحة كل قالب تحمل نسخة التنسيق داخلها (`<style>`) بجانب الوصل — فتفتح مصمَّمة
  من داخل الملف المضغوط ومن بعد الاستخراج معًا، ويظل `styles.css` مصدر التعديل الحيّ (وهو
  الأحدث في التسلسل فيغلب النسخة المضمّنة عند وجودهما). وعلامة التتبّع انتقلت **بعد** الـDOCTYPE
  لا قبله، فلا وضع quirks في أي متصفّح قديم. وعيّنتا `framework/` (Astro/Next) كانتا تطلبان
  `/styles.css` بمسار مطلق — صارا `./styles.css`.
- **إصلاح — لا تعليمات مطوّر في DOM المشتري**: «أضف أرقامك في content/profile.json» وأخواتها
  (الأعمال · الخدمات · مهارات السيرة — ومنها ما ظهر فعلًا في حزمة `mirrorbundle`) صارت فراغًا
  نظيفًا: القسم يبقى بعنوانه، ولا سطر «عدّل الملف الفلاني» في صفحةٍ مُسلَّمة ولا معاينةٍ حيّة
  ولا استضافة.
- **استوديو تخصيص القالب** (`/studio?tab=pkg`) للمشترين الموثّقين: رقم الطلب + مفتاح الرخصة
  يُطابقان سجلَّ الطلب كما يفعل زرّ التحميل (`verifyPurchase` في `src/data/pkgstudio.js`)،
  فتُفتح مساحة تحرير حيّة لنصوص الهوية والمدينة والأرقام، مع **رفع صور** (PNG/JPG/WebP حتى
  512 ك.ب) تُكتب في الحزمة `assets/img/` ببايتاتها الحقيقية ويُشار إليها بمسارات نسبية نظيفة
  (`./assets/img/…`)، و**إدارة أقسام** (إخفاء وإعادة ترتيب) تُطبَّق في الملفّات النهائية نفسها.
  وزرّ التصدير يبني ZIP مبنيةً كلّيًّا (`studioZip`) ببياناتك وصورك مطبوعة في `index.html`
  و`content/profile.json` — والرخصة والتتبّع باسمك ورقم طلبك. والمسودّة تُحفَظ لكل طلبٍ وقالب
  تحت `qalb.pkgstudio.v1`.
- **مقياسٌ واحد للـATS**: محتوى `<style>` (تنسيق الحزمة المضمَّن) يُستبعد قبل القياس في
  `analyzeAts` **وبالحرف نفسه** في السكربت المُسلَّم `scripts/check-ats.mjs` — فكلمات CSS لا
  تُحسب كلمات سير. شارات الأداء العشر على الرفّ مطابقة للقياس المعتمد بلا تغيير في أرقامها،
  وقواعد المهارات/النقاط استعادت صيغها الأصلية (سطر `<p>` لا `<li>` خارج التخطيط الجانبي).
- **`zip.js`** يقبل مدخلات ثنائية (`Uint8Array` للصور المكتوبة بلا ترميز) ويقرأها بـCRC
  (`zipBytes`) — فلا تُفسَد صورةٌ بتشفير نصّي.
- الاختبارات: مجموعةٌ جديدة من ٤٢ فحصًا — مسارات نظيفة في حزم القوالب الخمس عشرة كلها، ولا
  placeholder في أي صفحة مُسلَّمة، وعلامة بعد الـDOCTYPE في كل HTML، وصورة بالأحرف الست عشرة
  تصل ZIP سليمة الإشارة، والسقوف والوسوم في `sanitizeCustom`، والإخفاء والترتيب والأرقام
  مطبوعة في الملف النهائي، وبوابة الاستوديو بمصيدة المفتاح الخاطئ ثم التحرير الحيّ ورفع الصورة
  وتصدير ZIP حقيقي باسم الطلب. وكل المجموعات السابقة (76) وفحوصات API (268) تمرّ بلا كسر.

### جولة الواجهة وتجربة الشراء

- **بطاقة القالب** أُعيد بناؤها حقلًا حقلًا: الاسم في `<h3>`، والوصف في `<p>`، والسعر في
  `<span>` بصيغة «199 ر.س — ترخيص لمرة واحدة» (والسعر القديم مشطوبًا «بدل 249»)، والشارات
  (التصنيف · النوع · التقييم) في `<div>` مستقل، ثم زرّا «معاينة حية» و«أضف للسلة» في صفٍّ واحد.
  وبين كلِّ عنصرين مسافةٌ نصيّة حقيقية، فالقراءةُ الخطية (`textContent`، النسخ، محرّكات البحث)
  لم تعد تُخرج «أثيربورتفوليو…». و`Money` صار يطبع «199 ر.س» لا «199ر.س» في كل الموقع.
- **إصلاح**: البطاقة كانت تكتب «بعد الخصم» بجانب قالبٍ لا خصم عليه.
- **إصلاح**: «منظومة التوظيف9» في القائمة — الاسمُ والعدّاد صارا منفصلين.
- **هيكل التحميل** (`TemplateCardSkeleton`) نسخةٌ من البطاقة الجديدة بالأبعاد نفسها
  (اسم · وصف بسطرين · سعر · شارات · زرّان h-9)، ونتائجُ الكتالوج بارتفاعٍ أدنى ثابت
  فلا يقفز الفوتر (CLS). والإيصالُ أثناء الانتظار يرسم هيكله بدل دوّامةٍ و«جارٍ التحميل…».
- **الكتالوج بلا خادم**: في وضع rest، تعذّرُ `/catalog` كان يُبتلع بصمت؛ الآن سطرٌ يقول ذلك
  وزرُّ «إعادة المحاولة» (`catalogStatus` و`refreshCatalog` في المخزن). والكتالوج والرئيسية
  صارا يقرآن لقطة المخزن، فسعرٌ عُدّل من اللوحة يظهر فورًا بدل أرقام أول رسم.
- **الشريط العلوي**: «خصم 25% على حزمة «موقع + سيرة» وكل المتجر حتى 30 سبتمبر 2026 — كود SALE25»
  (النطاقُ كاملًا كما في السلة، لأن الكوبون يشمل كل المنتجات) —
  النسبة والأجل من جدول الكوبون، والكود ظاهرٌ على الجوال (كان مخفيًا)، والشريط يختفي وحده بعد
  الأجل فلا يُعلَن كوبونٌ يرفضه الدفع. وأُزيل «هذا الأسبوع» من عنوان «الأكثر رواجًا».
- **الترويسة والنشرة**: «بورتفوليو وسيرة ذاتية بهوية واحدة» / «…مع سيرة ATS جاهزة للتقديم.»،
  والنشرة: «اشترك لتصلك القوالب الجديدة ونصائح تجهيز سيرة وبورتفوليو احترافي.»
- **صفحة القالب**: مبدّلٌ بين «معاينة تفاعلية» و«الصفحة الحيّة» — الأخيرة هي `index.html` (أو
  `resume.html`) من الحزمة نفسها في iframe معزول (`sandbox=""`) بتنسيقها مضمّنًا، وبعرضٍ
  بالبكسل للجوال واللوحي فتعمل استعلامات الوسائط كجهازٍ حقيقي (`src/lib/live-page.js`).
  وأقسامٌ جديدة مقروءة من ملفّات التسليم: «ماذا ستحصل عليه» (موقع · سيرة ATS · خطاب · دليل
  التركيب · نسخ Astro/Next · تحديثات — كلٌّ بحسب ما في الحزمة فعلًا)، وشارات التقنيات،
  ومتطلبات النشر، وشارات الترخيص (شخصي ✓ · تجاري لعميل واحد ✓ · إعادة البيع ✗).
- **ضمانات الشراء** (`AssureRow`) بجانب أزرار الشراء في صفحة القالب والسلة والدفع:
  تسليم فوري بعد الدفع · ترخيص استخدام واضح · دعم عبر البريد · تحديثات مجانية.
- **`/templates/<slug>`** يُحوَّل إلى `/template/<slug>` مع الاستعلام والمرساة.
- **القانون**: صفحة `/licensing` لنص الترخيص (و`/licence` تبقى للتحقق من المفتاح)، والفوتر
  يربط الخمسة: الشروط · الخصوصية · الاسترجاع · الترخيص · التواصل.
- **إصلاح**: صفحة `/refunds` كانت تُرسم **بلا نصّ السياسة** (المسار يمرّر `refunds` والقسم
  معرّفه `refund`). و`sectionById` صار يقبل الصيغتين.
- **إصلاح**: بطاقات المشاركة `public/og/*.png` كانت بأسعارٍ قديمة (أثير 249 بدل 199، وسبعُ
  بطاقاتٍ أخرى) — أُعيد توليدها من الجدول.
- الاختبارات: مجموعةٌ جديدة من ٤٦ فحصًا تغطي كل ما سبق (منها: الشريط يختفي بتاريخٍ مزيّف
  بعد الأجل، وزرّ «إعادة المحاولة» يُزيل التنبيه حين يعود الخادم، و`/refunds` يطبع نصّه).

### أُضيف

- **الشعار والحقوق (White-label)**: سطرُ «صُنع بواسطة Qalb Store» برابطٍ نشط إلى
  الصفحة الرئيسية في فوتر كلِّ قالبٍ وكلِّ صفحةٍ منشورة — اللفظُ يُكتب مرةً واحدة
  في `badgeText` (`src/data/badge.js`) ويُطبع في الحزمة والاستضافة والشريط منه.
  وإزالتُه بطريقين صريحين: **رخصة White-label لمرةٍ واحدة بـ ‎50‎ ر.س** (إضافة
  `badge-off` بعد إعادة تسعيرها من ‎19‎) تُسقطه من ملفّات الـZIP وصفحة الاستضافة
  والشريط معًا، **واشتراك Qalb Pro يُسقطه مجانًا تلقائيًا** (ومعه Qalb Plus).
  والقرارُ كله في `badgeState` لا يتكرر: الحزمة (`deliverable.js`) والاستضافة
  (`hosting.js`) والاستوديو يسألونها جميعًا، فلا يُظهر وجهٌ ما يُسقطه آخر.
  وقلبُ الرخصة على موقعٍ مستضاف (`brandOff`) يتم من اللوحة بعد الطلب المدفوع —
  `PATCH /admin/sites/:slug` — والمتصفح يُجابه بـ ‎403 مثل ترقية الخطة تمامًا.

### تغيّر

- لفظُ الشارة صار «صُنع بواسطة Qalb Store» / “Made with Qalb Store” في كل مكان:
  فوتر القوالب المُسلَّمة، وشريط صفحات الاستضافة، ونصوص الاستوديو والاشتراكات.
- نقاطُ باقة Pro تذكر White-label صراحة بين ما تفتحه، وصفُّ المصفوفة يقول:
  تلقائيًا في Pro (وفي Plus) أو برخصة.
- `llms.txt` أُعيد توليده من الجداول: أسعار القوالب والاشتراكات والإضافات صارت
  تقرأ أرقام النسخة الحالية (وكان فيها أرقام جولاتٍ أقدم).

## [1.9.0] — 2026-09-24

جولةُ البيع: المتجر صار يقبض ويسلّم ويُصدر فاتورة. وقبلها: **بطاقةُ القالب أُعيد بناؤها**
— الاسمُ والوصفُ والسعرُ كلٌّ في سطره بفواصلَ مكتوبة، وزرّانِ صريحانِ («عرض القالب»
و«أضف للسلة») بحالةٍ بصريةٍ بعد الإضافة، وشارةُ «منظومة التوظيف» صارت «٩ أدوات» لا رقمًا
ملتصقًا. والعنوانُ الرئيسي يقود بالقيمة لا بالخصم: «بورتفوليو وسيرة ذاتية بهوية واحدة».
والخطط: **Plus ‎300‎ شهريًا / ‎3000‎ سنويًا · Pro ‎500‎ شهريًا / ‎5000‎ سنويًا** — رقمٌ واحد لكل سعر، يقرؤه المتجر والسلة والخادم من جدولٍ واحد. وأسعارُ القوالب: أثير وأتلييه ونِكسَس ‎199 / ‎249 / ‎199‎ بعد أن كانت ‎249 / ‎279 / ‎219‎.

### أُضيف

- **طبقةُ الدفع** (`server/payments.js`): محوّلٌ واحد بأربع حالات — Moyasar أو Tap أو
  Stripe، وتحويلٌ بنكيٌّ تلقائي حين لا مفتاح. المبلغُ من الطلب المخزَّن لا من المتصفح،
  والتأكيدُ من البوّابة نفسها (أو بتوقيع الإشعار)، ودفترُ المدفوعات سطرٌ لكل حدث بصلاحيات 0600.
- **الفاتورة** (`server/invoice.js`): `GET /orders/:id/invoice?key=…` — HTML مطبوع فيه
  السطورُ بأسعارها المختومة والخصم والضريبة المستخرجة من الصافي، ولا يُفتح بلا مفتاح الترخيص،
  وحقلُ البائع غير الموثَّق يُكتب مكانه «قيد التوثيق».
- **البريد** (`server/mail.js`): إيصالٌ عبر Resend، أو صندوقُ صادر على القرص حين لا بوّابة —
  فلا بريدَ يضيع بصمت، ولا وعدَ إرسالٍ بلا منفّذ.
- **النشرة والقالب المجاني** (`server/subscribers.js` + `/free`): بريدٌ مقابل طلبٍ صفريٍّ
  يُبنى من `makeOrder` نفسها بقسيمة ١٠٠٪ من الخادم — بترخيصٍ ورابط تنزيلٍ موقّع، ومرةٍ واحدة
  لكل بريد. والنشرةُ خانةٌ منفصلة، لا شرطًا على التسليم.
- **خطة النشر بـ ١٩**: بطاقةٌ في `/pricing` تقرأ رقمها من جدول الخطط نفسه.
- **حصّةُ ملفات المصدر** (`src/data/quota.js`): تصديرُ ملفات المصدر مفتوحٌ في Pro بسقفٍ
  معلن — **ثلاثةُ قوالب كل شهر**. القالبُ يُحصى مرةً واحدة في شهره (فالسقفُ على القوالب
  لا على النقرات)، والعدّادُ يُصفَّر مع أول يوم من الشهر، و`/account` يُري الباقي
  («٢ / ٣ هذا الشهر») ويُعطّل الزرَّ ببيانِ موعدِ التصفير حين تنفد الحصّة. وحين لا مفتاحَ
  لبوّابةِ دفع، يتحوّل الدفعُ إلى تحويلٍ بنكيٍّ برقمِ مرجعٍ هو رقمُ الطلب ومهلةِ ٤٨ ساعة.
- **الكوبون مؤجَّل**: `SALE25` يحمل `endsAt` ونطاقًا ونصًّا يشرح ما يشمله؛ يُرفض بعد أجله
  في السلة (`applyCoupon`) وفي الخادم (`recompute`)، والسلةُ تُخبر الزائر بالأجل وما يغطّيه.

### تغيّر

- بطاقةُ القالب: أربعةُ أسطرٍ (اسم · وصف · سعر · تصنيف) بفواصل «·» مكتوبة في النصّ،
  وسطرُ ترخيص، وزرّانِ بحالتين بصريّتين مختلفتين.
- شارةُ عدد أدوات التوظيف: «٩ أدوات» بدل «9» ملتصق بالعنوان.

### فحص

- ٧٥ مجموعة / ٥١ مسارًا في الواجهة (٣١٨ توقّعًا)، و٢٦٠ فحصًا لخادم الطلبات والمدفوعات
  والفاتورة والبريد — `npm test`. ومجموعةٌ جديدة «source export · three templates a month»
  بستةَ عشرَ تأكيدًا على الحصّة: من يملكها، ومتى تُغلق، ومتى تُصفَّر، وما تقوله الصفحة.

## [1.8.0] — 2026-09-24

حزمةُ اللمسات الأخيرة وطبقةُ الثقة. توحيدُ الدرجات في جدولٍ واحد: **مجاني 0 · Qalb Plus
19 شهريًا (149 سنويًا) · Qalb Pro 49 شهريًا (349 سنويًا)**، وزوالُ الخطة الفردية وباقةِ الشراء
لمرة واحدة. والمُنشئُ أُحيلَ إلى التقاعد (‎`/create` يحوّل إلى الكتالوج)، وبطاقةُ القالب فصَلت
الاسمَ والتصنيفَ والوصفَ كلَّ واحدٍ في سطرِه، وقسمُ «ماذا ستحصل عليه؟» يحمل أربعةَ مخرجات،
والهياكلُ العظمية حلّت محلّ رسائل التحميل، والفوترُ القانوني على كل صفحة — أربعُ صفحاتٍ مستقلة
‎`/terms · /privacy · /refunds · /contact` بالمراسلة والضريبة — وخصمُ SALE25 (25% حتى
30 سبتمبر 2026) يحمل السعرَ قبله وبعده على كل بطاقة.

### أُضيف

- **قسمُ «ماذا ستحصل عليه؟»**: `id="included"` في الرئيسية بأربعة مخرجاتٍ لكل قالب — موقعٌ
  جاهز للنشر، وسيرةٌ متوافقة مع أنظمة الفحص الآلي (ATS)، ورابطٌ حيّ ودعم النطاقات المخصصة،
  وترخيصٌ شخصي وتحديثاتٌ مجانية — وكلُّ بندٍ يروم إلى المكان الذي يسلّمه فعلًا.
- **`PlanCards`**: مكوّنٌ واحد لجدول الدرجات يستعمله ‎`/pricing` والرئيسية معًا — تبديلٌ
  شهري/سنوي، سطرُ الضريبة «شاملًا {v}»، وسنةٌ محسوبةٌ «بسعر {m} شهرًا» من الجدول لا من
  الذاكرة، وأزرارٌ تقود إلى مكانٍ يفعل الشيء: المجانية إلى الاستضافة، والمدفوعة إلى
  الحساب بخطته.
- **الهياكل العظمية**: `Skeleton` و`PreviewSkeleton` و`TemplateCardSkeleton` في
  `src/components/ui.jsx` تحلّ محلّ «جارٍ التحميل…» في بديل التوجيه وفي الكتالوج.
- **الصفحات القانونية الأربع**: `LegalDoc` يرسم قسمًا واحدًا لكل مسار من مصدرٍ واحد
  `src/data/legal.js` يُشاركه الفوتر وصفحةُ التوثيق الجامعة. صفحةُ المراسلة تبني بطاقتها
  من بيانات الشركة: البريد، والعنوان، والضريبة «15% مشمولة في السعر» — وما لا يوثَّق يُطبع
  «قيد التوثيق» لا رقمًا مُختَلَقًا.

- **`<meta name="robots" content="index, follow" />` صريحًا**: مكتوبٌ في `index.html` قبل أي
  JavaScript (تُجدّده كتلة `seo:generated` مع كل بناء)، ويكتبه `Seo.jsx` لكل مسار عام — فلا تبقى
  صفحةٌ بلا تصريح، ولا تعلق قيمةُ `noindex` بعد تنقّلٍ داخلي من السلة إلى صفحةٍ عامة.
- **`Product` كاملٌ في `/templates`**: `productItemList` يبني `ItemList` عناصرُه عُقَد `Product`
  لا أسماءً وروابط — لكل عنصرٍ `name` و`image` (بطاقةُ og الخاصة به) و`description` و`offers`
  بسعرٍ من بيانات الرّفّ نفسها وعملة `SAR` وحالة `https://schema.org/InStock`، مع `url` و
  `itemCondition` و`priceValidUntil` و`seller`. وعقدُ صفحة المنتج صار يحمل الحقولَ نفسها، ولا
  يُعلن صورةً بلا ملف.

### غُيَّر

- **جدولُ الدرجات الموحّد**: `PLANS` في `src/data/plans.js` صار مجاني 0 / Qalb Plus 19
  (149 سنويًا) / Qalb Pro 49 (349 سنويًا)، وزالت الخطة «الفردية» وباقةُ `ONE_TIME` من الجدول
  ومن مولّد `scripts/revenue-i18n.py`. والاستضافة (`src/data/hosting.js`) وجدولُ الإضافات
  (`src/data/upsells.js` حيث صار `pro-month` 49) يقرآن من المصدر نفسه، وقيدُ قاعدة البيانات
  `server/schema.sql` حدَّث مع الجدول.
- **مُنشئُ القوالب خرج**: ‎`/create` صار `<Navigate to="/templates">`، وكلُّ رابطٍ كان يقود
  إليه — شريطُ التنقل، والفوتر، وشريطُ صفحات الاستضافة المجانية (`brandBar`)، وزرُّ أداة
  ATS، و`llms.txt` — يقود إلى الكتالوج الآن، وحالةُ الحساب الفارغة صارت استمارةَ تسجيلٍ
  في الصفحة نفسها.
- **بطاقةُ القالب**: الاسم والتصنيف والوصف كلُّ واحدٍ في سطرِه، وسطرُ السعر مثبَتٌ في أسفل
  البطاقة بـ`mt-auto`، والسعرُ بعد الخصم والسعرُ المشطوب في السطر الواحد.
- **البطلُ كما كُتب في الطلب**: «أنشئ بورتفوليو وسيرة ذاتية / متطابقين في دقائق.» والزرّان
  «تصفح القوالب» و«معاينة القوالب المجانية» — وما عاد في أعلى الصفحة وعدُ شراءٍ لمرة واحدة.
- **القانوني صفحات**: `LegalDoc` يرسم قسمًا واحدًا لكل مسار — ‎`/terms` و`/privacy` و`/refunds`
  و`/contact` — وروابطُ الإقرار في الدفع وروابطُ صفحات التخصيص وB2B صارت تقود إلى المسارات
  الجديدة، والفوترُ القانوني على كل صفحةٍ مع شريطِ الضريبة (15% مشمولة في السعر).

- **الروابط صارت نصًّا خامًا في كل وسم**: `src/data/links.js` بُنّاءٌ واحد (`rawSite` · `rawUrl` ·
  `rawText`) يمرّ به `scripts/seo.mjs` و`scripts/agents.mjs` و`src/components/Seo.jsx` — فلا
  `marked()` ولا أيّ مُحوِّل Markdown في مسار الوسوم، ولا صيغةَ `[نص](رابط)` في `canonical`
  و`og:image` و`hreflang` ولا في `sitemap.xml` ولا في `llms.txt`. وقيمةُ `SITE_URL` الملصوقة من
  محرّر Markdown تُنزَع صيغتُها ويُبقى المضيفُ وحدَه (والمسارُ يسقط فلا يتضاعف `/templates/templates`).
  والنتيجةُ في الـHTML الخام: `https://qalb-store.vercel.app/templates`.
- **`llms.txt` بلا روابط Markdown**: كلُّ قالبٍ في النصفين صار سطرًا صريحًا
  (`- الاسم — https://…/template/slug — الوصف — السعر`) بدل `[الاسم](الرابط)`، والقاعدةُ مكتوبةٌ
  في رأس الملف — فقارئٌ لا يُحوِّل Markdown يرى العنوانَ نفسَه. و`robots.txt` يستعمل البنّاء نفسه
  في سطر `Sitemap`.
- **حاجزُ صيغةٍ يوقف البناء**: `scripts/seo.mjs` يفحص `sitemap.xml` و`llms.txt` و`robots.txt`
  و`index.html` قبل أن يطبع سطره، وأيُّ سطرٍ يحمل `[نص](رابط)` يوقف البناء باسم الملف ورقم السطر.
- **بطاقاتٌ تتمّدّ بارتفاعٍ واحد**: غلافُ `Reveal` صار `h-full` في شبكات الخدمات والعروض
  والرئيسية وصفحة المنتج، والبطاقةُ `flex h-full flex-col justify-between` فيصطفّ الزرُّ والصفُّ
  السفلي على سطرٍ واحد، وشبكتا الأسعار والاشتراك في الرئيسية تمدّان أعمدتَهما (زوال `items-start`
  الذي كان يمنع التمدّد).
- **لا تمرير أفقي من الهيكل**: `html` و`body` و`#root` تقصّ المحور الأفقي. القيمةُ المطلوبة
  `overflow-x: hidden` تبقى احتياطًا، و`clip` يغلبها حتى لا تسقط `position:sticky` في الشريط
  والأعمدة الجانبية. وهالةُ معاينة البطل في الرئيسية صارت `-inset-4` على الجوال و`-inset-10`
  من `sm` فما فوق، فلا تخرج عن عرض الهاتف.

### تختبره

`tests/smoke.mjs` — مجموعةٌ جديدة: نزعُ صيغة Markdown في البنّاء (`rawSite`/`rawUrl`/`rawText`)،
وسلامةُ الوسوم في `index.html` الخام، و`sitemap.xml` و`llms.txt` على القرص بلا قوسٍ واحد،
و`robots: index, follow` في صفحةٍ عامة، وشكلُ `Product` في `/templates` وفي صفحة القالب
(الاسم والصورة والوصف والعرضُ بالريال)، و`h-full` في أغلفة الشبكات و`justify-between` في البطاقات.
والمجموعاتُ الجديدة للدرجة الموحّدة: الجدولُ نفسه على الصفحةين (الأسماء والأسعار
والسنوات المحسوبة)، وقوائمُ `absent` الجديدة تحرّر كل حالةٍ من نصوص المُنشئ والباقة
المؤجّلة التي خرجت من المنتج، وحالاتُ ‎`/create` الثلاث صارت تفحص الكتالوج نفسه بعد
التحويل، والصفحاتُ القانونية الأربع دخلت الـsitemap مع سائر المسارات العامة (50
مسارًا)، ومجموعتا الاستضافة وAPI تثبّتان أن النطاق يكلف سعر Pro (49) لا السعرَ
القديم. المجموع: 72 مجموعةً و312 توقّعًا على 50 مسارًا في `npm test`.

## [1.7.0] — 2026-09-24

نموذجُ الربح. المتجرُ كان يبيع ملفًّا؛ صار يشغّل حسابًا: **قالبٌ أول مجانيٌّ كامل**، وما بعده خلف
اشتراك، ثم **سوقٌ للمصممين** بعمولة معلنة، و**خطُّ فحصٍ آليٍّ خماسي الطبقات** يحلّ محلّ مراجعة مئات
الملفات يدويًا. خمسُ صفحات، ووحدتا بيانات، ومولّدٌ، وبصماتُ صور، وطبقة خادم — كلها من أرقام
الجداول نفسها، فلا رقمَ مكتوبٌ مرتين.

### أُضيف

- **`/create` — معالج التوليد المجاني**: بريدٌ واحد وإقرارٌ بالفحص الآلي (`consent` بإصدار نصّ)، ثم
  اختيارُ المجال من ستة (مصمّمو المرحلة الأولى مُبرزون بلا شريط «قريبًا»)، ثم ستُّ إجاباتٍ قصيرة
  (`name/role/services/works/colours/links`) تُنقّى بحدودٍ واحدة في المتصفح والخادم، ثم **التوليد
  بمولّد الحزمة نفسه** (`sites.create` ← `renderSite`) فيخرج رابطٌ حيّ لا صورة. القالب الثاني
  يُوقفه المعالج نفسه: `canCreate` هي التي تقرأها الواجهة ويقرؤها الخادم، فلا بابان.
  و`#showcase` يعرض ثلاثة قوالب حقيقية بمعاينة المتجر مع تبديل سطح المكتب/اللوحي/الجوال.
- **`/pricing` — الخطط ومصفوفة القيمة**: مجاني (قالب واحد، علامة مائية) · فردية ٤٩/شهر و٣٩٩/سنة
  (٣ قوالب، تصدير نظيف، نشر على نطاق فرعي) · Pro ‏١٩٩/شهر و١٧٩٩/سنة (بلا حدود، نطاق خاص، بلا
  شارة، ATS). والمصفوفة عشرة صفوف، **كلُّ صفٍّ يحمل `enforcedBy` = «مسار:اسم الدالة» التي تُنفّذه**،
  والفحص يرفض أيَّ صفٍّ يشير إلى دالةٍ غير موجودة. وباقة لمرة واحدة ٣٤٩ وخدمة نشر ٤٩٩.
- **`/account` — العدّاد والعلامة المائية**: الحالة من `accountState`، وما تفتحه كل خطة من
  `gainedBy`، **وورقة السيرة تُبنى وتُختم بالعلامة المائية فعلًا** إن كانت الخطة مجانية
  (`withWatermark` — تُحقن بعد التصيير فلا تُخرّب `renderSite`)، فتُطبَع علامةً مائلةً باهتة لا
  تُخفى في الطباعة (`print-color-adjust`). فالوعد يُرى في المتصفح.
- **`/sell` — لوحة البائع**: التقسيم **يُحسب قبل البيع** — ١٩٩ ← عمولة ٢٥٪ = ٤٩٫٧٥ ← صافي
  ١٤٩٫٢٥ (بـ`dec` لا بـ`num`، فالهللة لا تختفي)، وتكلفة المعالجة تُعرض معلَّمةً تقديرًا لا خصمًا.
  وبصمات `pHash` لصور المعاينة، ورفعٌ يُشغّل الخط، وتقرير JSON بطبقاته وأوزانه وأسبابه، وإعادة فحص،
  وتظلّم، ومستحقات بمدة تأمين ١٤ يومًا وحدٍّ أدنى ١٠٠ ريال، ورسائل ما بعد القرار.
- **`/creators` — السوق**: لا يُعرض إلا `published`، والعمولة والتقسيم والترخيص ومدة التأمين
  معلنة قبل الشراء، وبلاغٌ (حقوق/برمجية/إساءة/أخرى) يجمّد الإدراج عند الثالثة.
- **خط الفحص الآلي** (`src/data/inspect.js` + `src/data/phash.js`): (١) تقني — امتدادات محظورة
  (`.exe` `.bat` `.php`…)، `eval`/`new Function`/`javascript:`، سكربتات خارجية، مسارات `../`، سقوف
  العدد والحجم، محرف null؛ (٢) بصمة بصرية — `aHash`+`dHash` ‏٦٤ بتًا ثابتة أمام السطوع وتغيّر
  المقاس، ومطابقة بالهامينغ (≥٠٫٩٠)، وبلا قارئ صور تُسجَّل «متجاوَزة بسبب» وتُضيف نقاطًا؛
  (٣) حارس سياسة بقواعد حتمية موزونة — ادّعاء تفضيل، ضمان مطلق، وعد بدخل، وسيلة تواصل مباشرة،
  **دفع خارج المنصة رفضٌ قاطع**، نصٌّ نائب، رقمُ نتيجةٍ بلا سند، حشو كلمات؛ (٤) القرار —
  ‏٠–٥٩ قبولٌ آلي، ٦٠–٨٤ حجرٌ مؤقت، ٨٥–١٠٠ رفضٌ نهائي، ومخالفة قاسية واحدة ترفع الدرجة إلى ٩٢؛
  (٥) شكاوى وتظلّم وتجميد.
- **`server/market.js` — طبقة السوق**: الحسابات والإدراجات والمبيعات والبلاغات والتظلّمات،
  و`GET|PATCH /admin/market` خلف الجلسة. **الفحص يُعاد تشغيله على الخادم**: إدراجٌ يحمل تقرير
  «accept» مُلفَّقًا يُفحص من جديد وتُشتق حالته من قرار الخادم. والخطة لا تُرقّى من المتصفح
  (٢٠٢ معلّق، والتفعيل يدوي)، والدفاتر الثلاثة ٠٦٠٠، وخناقٌ لكل صنف مسار لا حدٌّ واحد عام.
- **`server/schema.sql`**: جداول `accounts` / `account_templates` / `listings` / `listing_reports` /
  `listing_sales` بقواعد تحفظ ما يحفظه الخادم: `check (commission + seller_net = price)`، و`plan_pending`
  طلبٌ لا تفعيل، و`state` مشتقة، و`payout_summary()` تعيد حساب المتصفح نفسه.
- **٣٦٠ مفتاحًا × لغتين**، و`llms.txt` يحمل الخطط والعمولة وعتبات القرار أرقامًا مقروءةً بلا JavaScript.

### غُيِّر

- **شارة الصفحة المنشورة صارت دعوة**: «صُنع بقالب · **أنشئ نسختك مجانًا**» إلى `/create`،
  وخطة Pro تُسقطها كما كانت. وفاحص ATS المجاني يعرض في آخره توليد بورتفوليو كامل — الأداة
  المجانية بابٌ لا منافس.
- **`src/pages/Sell.jsx`**: أرقام التقسيم كانت بـ`num()` الذي يقرّب إلى ريال كامل فيُظهر ١٤٩٫٢٥
  ‏«١٤٩» ويخفي الهلالة عن البائع — صارت `dec()`.
- **وزنُ حارس السياسة أُعيد ضبطه**: نصوصٌ مثل «الأفضل عالميًا · ضمان ١٠٠٪ · دخل شهري · واتساب»
  كانت تسجّل ٥٢ فتُقبل؛ صارت ٦٤ فتُحجر. القواعدُ تُقرأ في الواجهة كما هي في الكود.

### مُصلَح

- **`dHash` على مصفوفة ٨×٨ تعطي ٥٦ بتًا لا ٦٤** — التوثيق كان يدّعي ٦٤؛ `hashMatrix` هي التي
  تُوسّع إلى ٩ أعمدة فتعطي ٦٤. والأرقام المعلنة على `/sell` صارت مشتقة من الدوال نفسها.
- **`droppedAnswers` كانت تقرأ مستوىً خاطئًا** من التداخل فتُظهر الحقول السليمة كمحذوفة.

### الاختبارات

- **٧٢ مجموعة / ١٢٨٨ فحصًا** (`npm test`): تصيير ٥٠ مسارًا (٢٨١ توقعًا) + ٢١ مجموعة تفاعلية
  (٧٧٣ فحصًا) في jsdom، ثم ٢٣٤ فحصًا لخادم اللوحة والتسليم والحسابات والسوق.
- مجموعة «نموذج الربح» (٦٩ فحصًا) تتحقق من: الخطط ضمن النطاقات المعلنة، وأن **كل `enforcedBy` من
  العشرة يشير إلى دالة موجودة فعلًا**، وأن `split()` يجمع إلى السعر بالضبط عبر مدى الأسعار،
  وبوابة `canCreate` ‏(١/٣/∞)، وعتبات العصابات، وأن نظيفًا يُقبل بصفر وأن نصًّا مشحونًا يُحجر،
  وأن **قاعدة قاسية واحدة تقلب قبولًا إلى رفض**، وثبات البصمة أمام السطوع والمقياس ورفض المقلوب،
  وتجميد البلاغ الثالث، والعلامة المائية في الطباعة.
- مجموعة السوق على الخادم (٢٥ فحصًا): رفض بلا إقرار، وبلا بريد صحيح، وتكرار البريد، وأن الخطة
  لا تُرقّى من المتصفح، وأن تقريرًا مُلفَّقًا يُنقض، وأن بلا حقوق أو بلا مفتاح لا يصل الخط،
  وأن البيع يُختم بالسعر المخزّن، وأن المرفوض لا يُشترى، وأن المجمّد يُسحب فورًا، وأن الدفاتر ٠٦٠٠.

### لم يُضَف بعد

- **لا بوابة دفع**: التفعيل والبيع **يُسجَّلان** ولا يُخصمان من بطاقة، والواجهتان تقولان ذلك صراحة.
  و`Stripe`/`Tap`/`Moyasar` مسجّلة أسماءً وحقولًا في التقسيم، لا اتصالات.
- **لا مُرسِل بريد**: فلا رسالة تأكيد ولا إشعارُ تفعيل — التسجيل يعيد المفتاح مرة واحدة.
- **لا نموذج لغة**: حارس السياسة قواعدُ حتمية موزونة، بلا استدعاء خارجي.
- **مطابقةُ الصور التجارية**: قائمةُ بصمات داخلية تُدار من `/admin/market`؛ ربطُها بقاعدة صور
  خارجية لم يُبنَ بعد.

## [1.6.0] — 2026-09-22

جولةُ منظومة التوظيف: الفاحصُ كان يجيب «هل تقرأ الآلة سيرتي؟»، وهذه الجولة تجيب السؤال الذي بعده —
«هل سيرتي مكتوبةٌ بلغةِ هذا الإعلان؟». ستُّ أدواتٍ جديدة حول التقديم الواحد، كلها تعمل في المتصفح
(لا حساب، ولا رفع، ولا إرسال)، وكلها تُسعَّر من `src/data/upsells.js` و`src/data/embed.js` كما تُسعَّر
القوالب: الخادمُ يعيد ختمَ السعر ولا يُصدِّق المتصفح.

### أُضيف

- **١. مطابقةُ السيرة بإعلان الوظيفة (`/match`)**: محرّكٌ واحد في `src/data/match.js` يستخرج من الإعلان
  ما يطلبه فعلًا — لا اسمَ الشركة ولا المدينة — ويوزن كلَّ مفردةٍ بتكرارها وموضعها (كلمةُ العنوان
  أثقلُ من كلمةِ المتن)، فتكون النسبةُ وزنَ الحاضر من وزنِ المطلوب. النسبةُ مجانية؛ والتقريرُ المفصّل
  (الكلماتُ الناقصة مرتّبةً بوزنها، وسطرٌ مقترحٌ لكلٍّ منها مع قسمه، وثلاثةُ قوالبَ من الكتالوج) بـ٢٩
  ريالًا، أو خمسةُ تقاريرَ بـ٧٩. المحرّكُ نفسُه يخدم `/kit` و`/embed`، فلا مقياسان في منتجٍ واحد.
- **٢. بطاقةُ جاهزيةٍ قابلة للمشاركة**: `src/data/share.js` يبني SVG في المتصفح (ورابطُ PNG عبر canvas،
  وإن غاب نُزّل الـSVG نفسه) من الدرجة التي قاسها الفاحص — لا رقمٌ تسويقي. نسخةٌ مجانيةٌ بشعارنا،
  ونسخةٌ «موثّقة» بلا شعار ومعها كوبونُ صديقٍ مشتقٌّ من الدرجة بـ١٩ ريالًا. تُعرض في `/ats` و`/match`.
- **٣. Qalb Link — رابطٌ مهني (`qalb.store/u/اسمك`)**: سيرةٌ وأعمالٌ وزرّا تواصل وتنزيل سيرة، وعدّادُ
  فتحاتٍ وتنزيلاتٍ وطلباتِ تواصل يراه صاحبُ الرابط وحده (`src/data/qalblink.js`). البلدُ مستنتجٌ من
  المنطقة الزمنية لجهاز الزائر لا من عنوانه، والصفحةُ تقول ذلك بصريح العبارة. الخطةُ المجانية تعدّ؛
  و«رابط بلس» (٢٩ في الشهر) يفتح البلدان والمصدر وسجلَّ ثلاثين يومًا والنطاق الخاص.
- **٤. مولّد ملف التقديم (`/kit`)**: من سيرتك وإعلانٍ واحد يخرج أربعةُ أشياء — نقاطٌ مرتّبة على مفردات
  الإعلان، خطابُ تقديم، رسالةُ LinkedIn، وإيميلٌ بموضوعه جاهز (`src/data/kit.js`). عشرُ توليدات بـ٤٩
  ريالًا، والرصيدُ يُمنح مرةً واحدة لكلِّ طلب يحمل الإضافة. وما لا يجده في سيرتك يتركه بين قوسين:
  لا يخترعُ خبرةً ولا رقمًا ولا اسمَ شركة — والفحصُ يُثبت هذا.
- **٥. فاحصٌ مضمّن للجهات (`/embed`)**: كود iframe واحد يضع الفاحص في موقعِ جامعةٍ أو معهدٍ أو مكتبِ
  توظيف: ١٩٩ ريالًا شهريًا لـ٥٠٠ تحميل، و٤٩٩ لـ٢٠٠٠، والجامعاتُ فوقها بالاتفاق. الذي يُباع موضعُ
  الفاحص وحصةُ تحميل، لا بياناتُ الطلاب — فالفحصُ يعمل في متصفح الزائر ولا نملك نتائجَه أصلًا.
- **٦. «ابنِ سيرتك من LinkedIn»**: لصقٌ لا وصول — لا نقرأ حسابًا ولا نطلب كلمةَ سر (`src/data/linkedin.js`).
  الأقسامُ تُقرأ (النبذة، الخبرة، التعليم، المهارات) وتُملأ الحقول، وما لم يُفهم يُسمّى ناقصًا ليُكمَل.
  القراءةُ مجانية؛ والملءُ هو ما يُباع (٣٩ ريالًا).
- **٧. دليل المواهب (`/talent`)**: ملفاتٌ فعّلها أصحابها من صفحة الرابط، تُبحث بالتخصص والمدينة ودرجةِ
  الجاهزية. الإدراجُ مجاني والإخراجُ بضغطة، والجاهزيةُ رقمٌ مقيس من سيرة صاحبها. الإبرازُ ٢٩ في الشهر،
  ووصولُ الشركات باشتراك.
- **٨. تقرير السوق المجهول (`/market`)**: أربعةُ أسئلةٍ اختيارية بعد كلِّ فحص (التخصص، المدينة، هل حصلت
  على مقابلة، أيُّ قالب) تُجمَع بلا اسمٍ ولا بريد، وكلُّ نسبةٍ تُنشر مع عددِ مَن خرجت منهم، وما دون
  خمسِ إجابات لا يُسمّى سوقًا. يُصدَّر CSV ويُباع للجامعات.
- **٩. شارة «بُنيَ بقالب»**: سطرٌ صغير في فوتر كلِّ قالبٍ منشور (`src/data/badge.js`)، تُسقطها خطةُ
  «قالب بلس» مجانًا أو إزالةٌ تُشترى بـ١٩ ريالًا لمرة — والصفحةُ تقول الطريقين فلا يشتري أحدٌ مرتين.

### غُيَّر

- **طريقةُ تسليمٍ ثالثة في جدول الإضافات**: إلى جانب «مهلةُ بريد» و«اشتراكٌ يُفعَّل» صار للإضافة أن
  تكون `instant` — تُفتح في المتصفح بعد الدفع (رصيدٌ أو ميزة) فلا تُوعد ببريدٍ لا يصل.
- **الفاحصُ يقبل التضمين**: `/ats?embed=1&org=…` يُسقط الترويسة والأسئلة ويُسجَّل تحميلًا على حسابِ
  الجهة، وهو ما يبيعه `/embed` فعلًا — لا كودٌ تسويقي بلا أثر.
- **`llms.txt` يصف المنظومة**: قسمٌ جديد («The hiring suite» و«منظومة التوظيف») يذكر الأسعارَ والحدود
  لقارئٍ آلي، كما يذكر أن الفاحص المضمّن لا يجمع بياناتِ طلاب.

### تختبره

`tests/smoke.mjs` — مجموعةٌ جديدة: `hiring suite · match, kit, link, directory, market, embed,
linkedin, badge` (٥٥ فحصًا) تُثبت أن السيرةَ التِقَنيّة تُطابق الإعلانَ التِقَنيّ فوق ٨٠٪ وسيرةُ المال
دونه بعشرين، وأن كلَّ نقطةِ تعديلٍ مشروطة، وأن الملفَّ لا يخترع رقمًا ولا شركة، وأن الرابطَ العربي
يصير مقبضًا لاتينيًا والبياناتُ الموسومة تُرفض بحقلها كلِّه، وأن البلدَ من المنطقة الزمنية لا من
العنوان، وأن نسبةَ السوق لا تُنشر قبل خمسِ إجابات، وأن الشارةَ تُطبع في الملف وتسقطها الإضافة،
وأن خطة «بلس» تُسقطها بلا شراء. ومعها ستةُ مساراتٍ جديدة في جدول المسارات و٣٩ مسارًا في الخريطة.

## [1.5.0] — 2026-09-22

جولةُ النمو: من متجرِ قالبٍ يُنزَّل إلى متجرٍ يبيع ما حول القالب أيضًا — خدماتٍ تُؤدّى، وتقريرًا يُشترى بعد
فحصٍ مجاني، واشتراكًا موازيًا للشراء لمرة واحدة، وعروضًا موسمية، وباقاتِ جامعاتٍ مُعاد تنظيمها. كلُّ سعرٍ جديد
من جدولٍ واحد (`src/data/upsells.js`) تقرؤه السلةُ والفاحصُ وصفحةُ الخدمات والخادم معًا — والخادمُ يعيد ختمه
ويرفض المجهول، كما يفعل مع القوالب منذ النسخة الأولى.

### أُضيف

- **إضافات Upsell في السلة والدفع (‎+39 إلى +249‎)**: أربعُ إضافاتٍ تُعرض في السلة وخطوة الدفع — خطابُ تقديمٍ
  مكتوب (‎39‎)، سيرةٌ مطابقةٌ للإعلان (‎79‎)، مراجعةُ ATS بشرية (‎149‎)، وتركيبُ الموقع على Vercel مع النطاق
  (‎249‎). إضافةٌ واحدة من كل نوع بلا كميات، تُحفَظ بمعرّفها لا بسعرها، وسعرُها يُقرأ من الجدول عند كل حساب.
  وطلبٌ من إضافةٍ وحدها (بلا قوالب) صار مسارًا صحيحًا: لا «سلة فارغة» ولا رفض من الخادم.
- **تقرير ATS تفصيلي مدفوع (‎29‎ ر.س)**: بعد الفحص المجاني تظهر بطاقةُ التقرير في `/ats` — تحليلٌ مكتوب لنصّ
  صاحبه، وترتيبُ إصلاحٍ بالأثر، مع منتقي مجالٍ يوصي بقوالب ذلك المجال من الكتالوج نفسه. زرُّها يضيفها إلى السلة،
  ومهلتُها (‎٤٨‎ ساعة عمل) مطبوعةٌ من الجدول لا مكتوبةٌ في الصفحة.
- **صفحة `/services` للخدمات المؤداة**: ثلاثُ خدماتٍ Done-For-You — تركيبُ الموقع (‎249‎، وهي الإضافةُ نفسها
  في السلة: منتجٌ واحد بسعرٍ واحد)، كتابةُ السيرة من الصفر (‎299‎)، والهويةُ كاملة (‎899‎). كلُّ بطاقةٍ تعلن
  مهلتها بالساعات، والصفحةُ تقول صريحةً أن التسليم بشريٌّ بالبريد لا تحميلٌ فوري.
- **اشتراك Qalb Pro في الرئيسية**: خطتان — ‎39‎ شهريًا و‎349‎ سنويًا — موازيتان لباقات الشراء لمرة واحدة،
  أسعارُهما ووعودهما من الجدول نفسه، ولا تجديدَ تلقائي: تذكيرٌ قبل النهاية والتجديدُ بيد المشترك (لا بوابةَ دفع
  هنا تُديم شيئًا، والصفحةُ تقول ذلك).
- **العروض الموسمية `/offers` و`/offers/:slug`**: أربعةُ مواسم من تقويم الطالب والخريج — التدريبُ التعاوني،
  يومُ المهنة، التخرج، وبدايةُ السنة الوظيفية — كلٌّ منها بصفحةٍ خاصة تُوصي بثلاثة منتجاتٍ حقيقية وتعمل بكوبون
  `FRIEND20`. «الموسمُ الحالي» يُوسَم من شهر اليوم (`activeOffer`)، لا من تاريخٍ يُكتب يدويًا.
- **كوبونات النمو**: `FRIEND20` خصمٌ مباشر ‎٢٠٪‎، و`COACH20`/`COACH30` رمزا مدرّبٍ — يُختم الرمزُ على سجلِّ
  الطلب فتُحسَب أثريةُ المدرّب من الدفتر وتُسوّى يدويًا، ولا عمولةَ تتحرك آليًا.
- **خمسة منتجات جديدة**: حزمةُ LinkedIn (‎119‎)، قوالبُ خطاب التقديم (‎49‎)، حزمةُ المستقلين (‎279‎، موقع
  وخدمات وأسعار وعقود)، وسيرتا تخصص — الطبي (‎119‎) والقانوني (‎109‎) — بشخصيات عرضٍ من مجالها (طبيبةُ
  امتياز ومحاميُّ تحكيم). كلُّها تدخل الكتالوج والخريطة وبطاقات og، ودرجةُ ATS على سيرتي التخصصين هي درجةُ
  الفاحص على السيرة التي يطبعانها فعلًا (‎100‎/‎100‎).
- **باقات الجامعات في `/b2b`**: ثلاثُ باقاتٍ بدل الخمس — فوجٌ (‎٥٠‎ مقعدًا، ‎7,500‎)، كليةٌ (‎١٠٠‎ مقعد مع
  لوحةِ المدرّب، ‎13,000‎)، وجامعةٌ (‎٣٠٠‎ مقعد وورشةُ عمل، ‎33,000‎). لوحةُ المدرّب هي صفحةُ رمز الفوج
  القائمة (ما استُهلك وما بقي، بلا أسماء طلاب)، والورشةُ بشريةٌ تُنسَّق بعد التوقيع، وما زاد على الثلاثِمئة
  يُقال له «عقدٌ ثانٍ» لا تُختلق باقةٌ أكبر.

### غُيّر

- **الخادم يسعّر الإضافات ويتحقق من الكوبون**: `recompute` في `server/worker.js` صار يقبل `addons` بجانب
  `lines` ويعيد ختم أسعارها من `src/data/upsells.js`، ويرفض الإضافةَ المجهولة كما يرفض القالب المجهول.
  والكوبونُ يُصدَّق من جدول القوالب: رمزٌ حقيقيٌّ بنسبةٍ مُختلَقة كان سيُمرَّر قبل اليوم، ورمزٌ مجهولٌ كان
  يُخزَّن بلا خصم — وكلاهما الآن مرفوض برسالةٍ باسمه.
- **عددُ القوالب التي يفتحها المقعد صار مشتقًّا**: كان «السبعة» نصًّا مكتوبًا في ترجمة الصفحة، ومع سيرتي
  التخصصين وحزمة المستقلين صار عشرة — فالعبارةُ الآن تُركَّب من `SEAT_TEMPLATES.length` في العربية
  والإنجليزية معًا.
- **`llms.txt` يصف المتجر الجديد**: الخدماتُ والاشتراكُ والمواسمُ والكوبوناتُ والإضافاتُ كلها فيه بأرقامها من
  البيانات، وسلّمُ الجامعات حلّ محل فقرة الباقة التجريبية، والفهرسُ يذكر `/services` و`/offers`.

### تختبره

`npm test`: ٤٩ مجموعة فحص · ٣١ مسارًا مُصيَّرًا · ١٦١ توقّعًا · و١٨ مجموعةً تفاعليةً بـ٦٠٩ فحوصٍ خضراء —
منها مجموعةُ «growth v1.5.0» الجديدة (‎36‎ فحصًا): جدولُ الأسعار حرفيًا كما اعتمدته الجولة، والإضافاتُ
تُضاف وتُحفظ وتُزال، وطلبٌ من إضافةٍ وحدها يمرّ من الدفع المحلي حتى `qalb.lastOrder`، وخطتا Pro
وحسابُ توفير السنة، وتقريرُ ATS المدفوع ومنتقي المجال، وصفحتا الخدمات والمواسم، وزرُّ «أضف المجموعة»
يزرع الحزمةَ كلها في السلة. وفي الخادم (`node tests/admin-api.mjs`): ٢٠٤ فحوصًا · ٠ فشل — طلبُ
الإضافة وحده 201، والسعرُ المُدّعى 400، والإضافةُ المجهولة والكوبونُ المجهول بالاسم، وCOACH30 يختم
‎30%‎ من الجدول. و`npm run lint` صفرُ مشكلة، و`prettier --check` نظيف، و`gen:seo` ٣٩ رابطًا و٢٠ بطاقة og.

### أُصلح

- **زرّ «إتمام الشراء» في سلةٍ من إضافةٍ وحدها**: مسارُ الشراء كان يفترض قالبًا واحدًا على الأقل — طلبُ
  التقرير المدفوع وحده كان سيقع في «سلتك فارغة». صار العدُّ للقوالب والإضافات معًا في السلة والدفع وشارة
  السلة وعدد الطلب.
- **عرضُ الخدمة في الإيصال بلا زر تحميل**: إضافاتُ الطلب (خدماتٌ واشتراكات) تُعرض في صفحة النجاح بمهلتها أو
  بملاحظة تفعيلها — لا زرَّ «تحميل الحزمة» لما لا يُنزَّل.
- **زرّ «أضف المجموعة إلى السلة» في صفحات المواسم كان يضيف آخرَ منتجٍ فقط**: ثلاثُ إضافاتٍ متتابعةٍ تقرأ
  كلٌّ منها نسخةً قديمةً من السلة فيسقط ما قبل الأخير. صار للمخزن `addMany` تحديثٌ واحدٌ ذرّيٌّ يضيف
  المجموعةَ كلها.

## [1.4.0] — 2026-09-22

حزمةُ ظهورٍ في البحث وانضباطٍ على الجوال. النطاقُ صار مصدرًا واحدًا: canonical وog:image وsitemap
كلُّها من سطرٍ واحد قابلٍ للتبديل، فأُصلحت صورةُ og:image الميتة وتوحّد canonical مع الخريطة. وhreflang
للنسختين مع `?lang=en` الذي يفتح الإنجليزيةَ فعلًا لا اسمًا فقط. وبياناتٌ منظّمةٌ أغنى: Product مع
BreadcrumbList، وFAQPage للرئيسية من أسئلتها نفسها، ومدوّنةٌ بثلاثة مقالات تدخل الخريطة و`llms.txt`.
وعلى الجوال: الـaside المُغلقُ يُخفى فلا يجرّ التجاوز الأفقي، وشريطُ الإعلان يلتفّ لا يُقطع، وبطاقاتُ
القوالب تتساوى ارتفاعًا.

### أُضيف

- **`hreflang` للنسختين و`x-default`**: ثلاثةُ وسوم لكل مسار — `ar` على المسار نفسه، و`en` على
  `?lang=en`، و`x-default` يشير إلى العربية — تكتبها `Seo.jsx` مع كل تنقّل، ونسخةٌ ثابتة منها في
  كتلة `index.html` للمُعطَّل عنده JavaScript، وثالثة في `sitemap.xml` نفسها (`xhtml:link` لكل رابط
  من الروابط الثمانية والعشرين). والوعدُ مُنفَّذ: معاملُ `?lang=en|ar` يقرأه مسبّقُ الرسم في
  `index.html` و`LangProvider` معًا، فتفتح النسخةَ المذكورة في hreflang فعلًا.
- **بياناتٌ منظّمةٌ أغنى**: `Product` + `BreadcrumbList` في صفحة المنتج، و`ItemList` + `BreadcrumbList`
  في الكتالوج، و`FAQPage` على الرئيسية — أسئلتها الستةُ هي أسئلةُ الصفحة نفسها حرفيًا كما في فاحصي
  `/ats` و`/b2b` — و`Blog` + `BlogPosting` في المدوّنة، و`sameAs` بحسابات التواصل في `Organization`.
  والبنيانُ `@graph` واحد يجمع العُقد، فأُضيفت المساراتُ دون أن تُزاحَ العُقد القائمة.
- **مدوّنة `/blog` و`/blog/:slug`**: ثلاثةُ مقالاتٍ عملية بنسختَي اللغتين (`src/data/posts.js`): قواعدُ
  ATS السبع، والبورتفوليو مقابل السيرة، والنشر في عشر دقائق. كلُّ سطرٍ فيها مسنودٌ إلى ما يقيسه
  `analyzeAts` فعليًا وإلى مسار التسليم الحقيقي — لا نصٌّ تسويقي بلا سند. وتدخل المقالاتُ الخريطةَ
  (٣ روابط) وفهرسَي `llms.txt` وصفحةَ الرئيسية من الفوتر، ومقالُها الواحد يحمل `BlogPosting` بتاريخه
  ومساره.
- **`link rel="sitemap"` و`meta keywords`** في `index.html`، و`preload` لثلاثة خطوط حرجة (`crossorigin`
  معها كما يقتضي الخطُّ المحمَّل مسبقًا): نسختا IBM Plex Sans Arabic العربية واللاتينية وTajawal ٨٠٠
  للعناوين — مع تعليقٍ يربطها بـ`src/fonts.css` ليُحدَّثها من يشغّل `gen:fonts`.
- **حساباتُ تواصل حقيقية**: `SOCIAL` في `src/data/contact.js` — الأيقونات الأربع في التذييل تفتح
  المنصاتَ في تبويبٍ جديد (`rel="noopener noreferrer me"`) بدل `#contact`، و`aria-label` باسم المنصة
  لا بحرفها المختصر. والروابطُ تُضبط مرةً واحدة من `VITE_QALB_X|_IG|_LI|_BE` في `.env`، وهي نفسها
  ما يظهر في `sameAs`.

### صُحِّح

- **صورةُ og:image الناطقة عن نطاقٍ لا يعمل**: صار `og:image` و`twitter:image` يُكتبان من `SITE_URL`
  نفسه في المسار المطلق الصحيح (`/og-cover.png` و`/og/<slug>.png`)، وكتلةُ `index.html` بين علامتَي
  `seo:generated:start/end` **تُعاد كتابتها مع كل بناء** — كانت تُحقن مرةً واحدة ثم تتجمّد على أول
  نطاقٍ كُتب يومها، فتبقى صورةً قديمةً بعد كل تبديل. والتبديلُ كله صار خطوتين: `SITE_URL` +
  `VITE_SITE_URL` في `.env` ثم `npm run gen:seo`.
- **التجاوز الأفقي على الجوال**: درجُ القائمة ولوحةُ الفلاتر كانا يبقيان مدلّيَين خارج الشاشة
  (`translate-x-full`) وهو مغلق، والمتصفّح يزيح الصفحة أفقيًّا خلفهما. صار كلٌّ منهما يُخفى
  بـ`visibility` عند الإغلاق (والانتقالُ `[transform,visibility]` يُبقي حركةَ الانزلاق كاملة)، فلا
  تدلّى ولا رسمَ خارج الحاوية، ولا يبقى شيءٌ منهما في شجرة قارئات الشاشة ولا في مسار Tab — وقد
  أُزيل `aria-hidden` عن حاويتَي الدرجَين اعتمادًا على ذلك، فبقي `aria-hidden` على الزخرفيِّ وحده
  (الأيقونات والنجوم المرسومة)، والتقييمُ بالنجوم صار `role="img"` يُعلن قيمته ولو خُفي الرقم.
- **قطعُ نصّ شريط الإعلان على الجوال**: `truncate` كان يقتطع نصَّ الشريط بنهايةٍ مبتورة. مُزال —
  والشريطُ يلتفّ في سطرين بخطٍّ أصغر تحت `sm`، والكودُ والرابطُ يبقيان مخفيَّين في شاشاتهما كما كان.
- **ارتفاعُ بطاقات القوالب في الشبكة**: الغلافُ `Reveal` لم يكن يمتدُّ لارتفاع خليته، فتبقى البطاقة
  على ارتفاع متنها ويتراخى صفُّ السعر. صارت البطاقة `h-full` مع `Reveal className="h-full"` في
  الشبكات الثلاث (الرئيسية والكتالوج والمفضلة)، وصفُّ السعر بـ`mt-auto` يلتصق بالأسفل فتتساوى
  بطاقاتُ الصف الواحد بغضّ النظر عن التفاصيل.

### تغيّر

- **`sitemap.xml`**: ٢٨ رابطًا (٩ صفحات + المدوّنة + ١٥ منتجًا + ٣ مقالات) وكلٌّ منها يعلن
  بدائله الثلاث بـ`xhtml:link`. و`llms.txt`: فهرساه (الإنجليزية والعربية) يربطان المدوّنة. والثلاثةُ
  من مولّدٍ واحد، والفحصُ يعيد توليدها ويقارنها بما على القرص سطرًا بسطر كما كان.
- **canonical و`og:url`**: من `window.location.origin` إلى `SITE_URL` — فالصفحةُ المفتوحة من نطاق
  معاينة تعلن مسارها على النطاق الأساسي نفسه الذي تنشره الخريطة، وهذا هو توحيدُ canonical مع
  sitemap بعد أن كانا ينفصلان على كل معاينة.

### تختبره

`npm test`: ٤٨ مجموعة · ٣١ مسارًا · ١٦١ توقّعًا — منها مساران جديدان (`/blog` و`/blog/seven-ats-rules`)
والمجموعاتُ التفاعلية سبعَ عشرة كلُّها خضراء. وفيها فحوصاتٌ جديدة صراحةً: breadcrumb يرافق
المنتج، وhreflang الثلاثة على كل مسار مع `?lang=en` في رابط en، وcanonical من `SITE_URL` لا من أصل
التصفّح، و`FAQPage` يسأل حرفيًا ما يسأل عنه `#faq`، و`sameAs` أربعة روابط `https` في Organization،
وعددُ `<loc>` في الخريطة = صفحات + منتجات + مقالات و`x-default` بعدد الروابط نفسها. و
`node tests/admin-api.mjs`: ١٩٦ فحصًا · ٠ فشل. و`npm run lint` صفرُ مشكلة، و`prettier --check` نظيف.

### معلَّق عمدًا

- **DNS نطاق `qalb.store`** يضبطه المالك من لوحة المزوّد (CNAME إلى `cname.vercel-dns.com`)؛ والكودُ
  كلُّه جاهزٌ على افتراضه، ومبدَّلٌ إلى أيِّ نطاقٍ آخر — رابط vercel.app مثلًا — بسطرَين في `.env`
  ثم `npm run gen:seo`.
- **روابطُ التواصل** الآن روابطُ العلامة المبدئية (`x.com/qalbstore` …) إلى أن تُضبط `VITE_QALB_*`
  بحساباتٍ حقيقية؛ والأيقوناتُ لا تدّعي خلاف ما تفتحه.

## [1.3.3] — 2026-09-08

تحسينُ ما يقرؤه الوكيلُ الآلي عن الموقع: السماحُ الصريحُ لوكلاء الذكاء الاصطناعي، وملفُ
`llms.txt` ملخّصٌ ثنائيُّ اللغة، وكلاهما من مولّدٍ واحدٍ لا من نسخةٍ يدويةٍ تنجرف.

### أُضيف

- **`public/robots.txt` يعيد التوليد**: كتلةٌ مستقلة لكلِّ وكيلٍ من ثمانيةَ عشرَ وكيل ذكاءٍ
  اصطناعي (`GPTBot`, `OAI-SearchBot`, `ChatGPT-User`, `ClaudeBot`, `Claude-User`, `anthropic-ai`,
  `PerplexityBot`, `Perplexity-User`, `Google-Extended`, `Applebot-Extended`, `Meta-ExternalAgent`,
  `Meta-ExternalFetcher`, `CCBot`, `cohere-ai`, `DuckAssistBot`, `MistralAI`, `MistralAI-User`,
  `Amazonbot`) وفيها `Allow: /` — لأنَّ السماحَ الضمنيَّ من `User-agent: *` لا يقرأه فاحصٌ يبحث
  عن نصٍّ صريح. والمنعُ نفسه في كلِّ كتلةٍ بلا تفاوت: `/checkout` و`/order` و`/cart` و`/admin`
  و`/studio`؛ فلو وُضع منعٌ عند `*` وحده لمرّ وكلاءُ الذكاء الاصطناعي فوقه.
- **`public/llms.txt`**: ملخّصٌ مسطّحٌ للمتجر — إنجليزيٌّ كاملٌ ثم عربيٌّ كامل — يسمّي القوالبَ
  الخمسةَ عشر بروابطها وأسعارها، ويفصلُ السبعةَ التي تحمل سيرةً بمعايير ATS، ويشرح الفاحصَ المجاني
  (٧ قواعد، من مئة، لا حسابَ ولا رفعَ ملفٍ ولا تخزين، حدُّ العبور ٧٠)، ثم قسمُ المؤسسات: الشرائحُ
  الخمسُ بمقاعدها وأسعارها وفارقِها عن الشراء الفردي، وقياسُ الدفعة، وورقةُ العرضِ بما فيها من
  رقمٍ وصلاحيةٍ وفصلِ ضريبة.
- **صدقُ الملفِ مفحوصٌ لا مقصودٌ**: لا رقمَ ضريبيًّا ولا IBAN ولا سجلًّا تجاريًّا يُطبع ما لم
  يُضبط (`pending verification` / «قيدَ التوثيق» بدلَ رقمٍ مُختلَق)، و«لسنا مُدرَجين في اعتماد بعد»
  ما لم يوثِّق المالك خلافَها، ولا هاتفَ ولا رابطَ حجزٍ يُذكر إن لم يكن مضبوطًا، وعبارةُ «لا بوابةَ
  دفع» و«لا نُصدر فاتورةً إلكترونية ZATCA» مكتوبتان في اللغتَين. الفحصُ يعيد توليدَ الملفين ويقارنهما
  بما على القرص سطرًا بسطر، ويعدُّ كلَّ كتلةِ وكيلٍ فيها المنعُ نفسُه، ويقرأُ السقفَ والحدَّ من البيانات
  نفسها التي يقرأهما المولّد.
- **سقفُ قياسِ الدفعة صار رقمًا واحدًا**: `COHORT_MAX = 500` في `src/data/b2b.js` — الصفحةُ تلتزمُ به
  وتُنبّه عند التجاوز (`b2b.cohortCapped`)، و`llms.txt` يقتبسُه من نفس السطر. كان الوصفُ يقول «حتى
  ٥٠٠ سيرة» ولا شيءَ في الكود يُلزِم؛ صار وعدٌ بلا سندٌ واحدٌ أقلّ.
- **`vercel.json`**: سطرُ `headers` للمسارات الثلاثة (`robots.txt`، `sitemap.xml`، `llms.txt`)
  بـ`Cache-Control: public, max-age=0, must-revalidate` و`nosniff`، فلا تُخدَم كـHTML ولا تُجمَّد
  خلف وسيط؛ و`_redirects` باقٍ لغير Vercel.

### تختبره

`npm test`: ٤٦ مجموعة · ٢٩ مسارًا · ١٥٤ توقّعًا، منها مجموعةٌ جديدة «agents · robots and llms»
بسبعةٍ وعشرين إقرارًا (تطابقُ القرصِ مع المولّد، كتلةٌ لكلِّ وكيلٍ ومنعٌ واحدٌ للجميع، الأرقامُ
مقروءةٌ من `templates` و`B2B_TIERS` و`ATS_TARGET`، لا تسرّبَ سرًّا في الملفين). و`node tests/admin-api.mjs`:
١٩٦ فحصًا · ٠ فشل. `npm run build` ثم معاينةُ الحزمة: `/llms.txt` و`/robots.txt` يرجّعان ٢٠٠
بـ`Content-Type: text/plain`.

## [1.3.2] — 2026-09-06

أربعُ ملاحظاتٍ على `/b2b` كانت تُسقط العميلَ قبل أن يصل: الحقولُ لا تُحفَظ، ولا عرضَ سعرٍ رسمي، ولا هاتف،
وسعرُ المقعد يبدو ضعفَ التجزئة. صار لكلٍّ منها backing.

### أُضيف

- **دفترُ الطلبات** (`src/data/leads.js` · `server/leads.js` · `src/api/leads.js`): ما يُكتب في الاستمارة
  يُحفَظ فعلاً. `POST /leads` عامٌّ (خمسُ طلباتٍ في الدقيقة لكل عنوان، سقفُ جسم ٢٤ك) يكتب في
  `server/leads.json` بوضع ٠٦٠٠ ويُعيد رقمَ عرضٍ `QALB-Q-<سنة>-<٤>` مشتقًّا من الطلب نفسه — فالرقمُ الذي
  يراه المشتري هو الرقمُ الذي في الدفتر. التكرارُ من نفس الجهة والباقة في نفس اليوم جوابُه ٢٠٠ لا صفٌّ ثانٍ.
  في الوضع المحلي دفترُ المتصفح `qalb.leads.v1` (أربعون طلبًا) مع نصٍّ يقول إنه لا يُرى عندنا، وزرُّ تصدير
  CSV ومسحٌ حقيقيّ للحقلين و`localStorage` معًا — ويقول ما لا يستطيع مسحَه: صفوفُ الخادم.
- **ورقةُ عرضِ سعرٍ تُطبع PDF**: الأساسُ والضريبةُ والإجماليُ في ثلاثة أسطر، ورقمُ السجل والرقم الضريبي
  وIBAN والعنوان، وصلاحيّةٌ ثلاثون يومًا، وخانةُ «منشأةٌ في منصة اعتماد». البياناتُ القانونية لا تُختلَق:
  مصدرُها `src/data/company.js` من `VITE_QALB_LEGAL_NAME|_CR|_VAT|_IBAN|_BANK`، وكلُّ حقلٍ غير مضبوط
  يُطبع «قيد التوثيق» وتُطبع تحته قائمةُ ما ينقص. وفي الطباعة لا يبقى على الورق غيرُ الورقة:
  `[data-no-print]` و`body:has([data-b2b-quote]) .page-x > *:not([data-b2b-quote])`.
- **باقةُ دخولٍ تجريبية** ٢٥ مقعدًا / ٣٩٠٠ ريالًا (١٥٦ للمقعد، تحت توقيع العميد) مع رصيدٍ يُخصم عند الترقية
  يطبّقه الموظف من اللوحة (`PATCH /admin/leads/<quote>` بحقل `credit`) — لا خصمَ ذاتي قبل بوابة دفع.
- **سعرُ المقعد أُعيدت حسبته إلى ما يفتحه**: `seatBundle()` يقرأ المرجعَ من الكتالوج نفسه (٤٤٩ ريالًا لقالبٍ
  يحمل سيرةً ذاتية) فتنخفض الأرقامُ المعروضة إلى ٣٣٪/٦٤٪/٦٥٪/٧٦٪/٨٠٪ تحت سعر الفردية، ويُطبع النطاقُ
  ٥٩–٤٤٩ معدودًا من السبعة القابلة للقياس (`seatRetailBand()`)، لا مكتوبًا في جملة. وحاسبةُ مقاعد: اكتب
  ٦٠ فيُقال لك ١٥٠ (+٩٠) بزرٍّ يربط الاختيار بالباقة (`tierForSeats`).
- **الدفعةُ تُقاس لا تُوصف**: `data-b2b-cohort` يلصق حتى ٥٠٠ سيرة مفصولة بـ`---` فيُعيد متوسطًا وعددَ
  العابرين حدَّ ٦٠ ومجموعَ الفجوات، ويصدّر CSV بالنتائج وحدَها — النصُّ لا يُخزَّن ولا يُرسَل. السكربتُ هو
  `analyzeAts` المشحون نفسه، لا نسخةٌ تسويقية منه، والصفحةُ تعرض العقدَ قبل/بعد كأرقامَ لا كشعار.
- **هاتفٌ وموعدُ عرضٍ من عشرين دقيقة**: `VITE_QALB_PHONE` و`VITE_QALB_BOOKING` (و`VITE_QALB_ETIMAD`
  لرقم المنشأة في اعتماد). لا رقمٌ مضبوط؟ لا زرَّ «اتصل» ولا ميعاد وهمي — تُطبع الجملةُ التي تقول ذلك.
- **لوحةُ الإدارة**: تبويبُ «الطلبات» بجانب المؤسسات — حالةٌ لكل طلب (`new/contacted/quoted/won/lost`) وملاحظةٌ
  للموظف وحده والائتمانُ المطبَّق، وتصديرُ CSV باثني عشر عمودًا؛ وفي الوضع المحلي تُصدَّر نسخُ هذا المتصفح
  مع التنويه، ولا تُدّعى قراءةَ دفترٍ لا وجود له.

### تختبره

`node tests/admin-api.mjs`: ١٩٤ فحصًا (منها ٢١ لعقد `/leads`: ٢٠١/٢٠٠، ٤٠٠ بالأسماء، ٤١٣، ٤٠1 بلا جلسة،
PATCH مقبول ومرفوض، CSV، ٤٢٩، ووضعُ الملف ٠٦٠٠ من `/health`). و`npm test`: ٤٥ مجموعة · ٢٩ مسارًا ·
١٥٤ توقّعًا، منها أن ما يُكتب في الحقل يصلُ إلى البريد والورقة والدفتر بثلاثة أرقامٍ لا رقمٍ واحد،
وأن «متوسطَ الدفعة» المطبوع هو ما يرجّعهُ الفاحصُ فعلًا.

### معلَّق عمدًا

بوابةُ دفعٍ للترقيات · فاتورةٌ إلكترونية ZATCA · واجهةُ مقعدٍ لكل طالب على حدة · خادمُ بريدٍ حقيقي
(المراسلةُ حتى ذلك الحين بـ`mailto` إلى `qalb@qalb.store`) · إعلانُ رقم المنشأة في اعتماد بعد ضبطه.

## [1.3.1] — 2026-09-05

إصلاحُ خمسِ ملاحظات مستعجلة، كلُّها مما تراه العين قبل أي إعداد.

### أُصلح

- **اهتزازُ المعاينة** (`c4ef930`): كان الصندوقُ يقيس العرضَ بكسورٍ البكسل ويكتب ارتفاعًا من JS، فكلُّ
  إطلاقٍ لـ`ResizeObserver` — حتى ذلك الذي سببُه ارتفاعٌ لا عرض، أو شريطُ تمريرٍ فتحَه الصفحةُ
  التي أطالها القالب — يعيد الضبط فيعيد القياس. الآن خطّافٌ واحد مشترك `useFitWidth()`: تقريبٌ إلى
  أقرب بكسل، ولا تحديث إلا عند تغيّرٍ حقيقي، و`resize` احتياطًا حيث لا `ResizeObserver`. لا ارتفاع
  مكتوبًا من JS بعد اليوم: `aspect-ratio` في CSS للسيرة وللموقع معًا. والقاعدتان في مكان واحد:
  `@utility fitbox` (`overflow: hidden` + `contain: layout paint`) و`@utility fitscale`
  (`will-change: transform`)، مع `scrollbar-gutter: stable` على `html` فلا يفتح شريطُ تمريرٍ حلقة.
- **خريطة المسارات على Vercel**: `vercel.json` بمحتًاه الحرفي، فـ`/ats` و`/admin` و`/studio` تصمد
  عند إعادة التحديث القسرية بدل صفحةٍ بيضاء؛ و`public/_redirects` باقٍ لـ Netlify وCloudflare Pages.
  الفحص يقرأ الملفين ويثبت أن النمط يبتلع المسارات المخفية كلها.
- **النزول إلى `#bundles`**: كان `ScrollManager` يمرّ إلى `scrollTo(top)` إذا لم يكن القسمُ مركَّبًا
  بعد (تحميلٌ كسول)، فترتدّ الروابطُ إلى السقف. الآن محاولةٌ حتى ٢٤ إطارًا، و`behavior` يقرأ
  `prefers-reduced-motion`، ومستمعُ `click` في طور الالتقاط ينزل بك إلى القسم حتى لو لم تتغيّر
  الوجهة؛ و`scroll-margin-block-start: 5.5rem` يخلي رأسَ القسم من تحت الترويسة اللاصقة.
- **رابطُ «لوحة الإدارة» خرج من فوتر العموم** وبقي المسارُ محجوبًا كما كان (`noindex` + robots +
  فحصُ المسارات الخاصة)، وحُذف مفتاح `footer.admin` لأنه لم يعد له قارئ. لا شيء في اللوحة يُفتَح
  بنقرةٍ عابرة من صفحة بيع.
- **سطرُ الإنجليزية في بطاقة الماسح** صار `hero.scan1..4` بالعربية والإنجليزية، لا نصًّا مكتوبًا في
  الصفحة: لا يرى الزائر جملةً أجنبية في نصف عربي خالص.
- **العنوانُ البيعيّ الأول**: «القالب ملكك مدى الحياة — الاستضافةُ اختيارية»، و«من باقة ١٩ ريالًا»
  وُصِفت في صفحة الاستضافة بأنها **استضافةٌ مُدارة اختيارية** (مفتاحٌ جديد `host.managedOptional`؛
  `host.optional` كانت تسميةَ حقلٍ في النموذج فلم تُلمس).
- **شارةُ «المسار الكامل» وسعرُه مشتقّان**: العددُ `templates.length` كما هو اليوم (١٥)، والسعرُ جمعُ
  أسعار القوالب كلِّها (٣٬٥٢٥ ريالًا) — وهو ما تحسبه السلة فعلًا، لا رقمٌ مختلفًا عنه. و`duo` و`single`
  يشتقّان بالطريقة نفسها (٤٤٩ و١٤٩ كما كانتا). حُذف من الوصف وعدُ «جلسة LinkedIn» الذي لا يملكه
  أحدٌ عندنا: المراجعةُ البشرية تُطلب بالبريد، وهذا ما يقوله النصّ الآن.
- **فاحصُ ATS يبيع ما قياسُه**: `atsReadyTemplates()` بحدٍّ مسمّى في وحدة البيانات
  (`ATS_STORE_MIN = 97`) بدل رقمٍ مكرَّر في الصفحة، وختامُ التقرير يقرأ درجتك: تحت الحدّ القالبُ
  واحدٌ بالسعر الحقيقي، ومن الحدّ إلى ٩١ تُعرض الهويةُ لا الإنقاذُ مع أرخص ثلاثة قوالب مقيَسة
  ورابطٍ إلى الفهرس مفلتَرًا إلى تلك المعرّفات بعينها، وفوق ٩١ يُقال لك صراحةً إن لا قالبَ يضيف ما
  نقصك. ووُطّن نصُّ البطاقة في اللغتين، و«عشر قواعد» صارت سبعًا كما يثبّت الفحص في اللغتين.

### الاختبار

`npm test`: ٤٥ مجموعة (٢٩ مسارًا / ١٥١ توقّعًا؛ home ٢٧، css ١٧، ats ٥٨ بعد أن كانت ٢١ و١٢ و٥٢)
و١٧٣ فحصًا لخادم اللوحة بلا فشل · prettier وeslint صامتتان · البناء و`seo: ٢٤ رابطًا` ✓ ·
٧٦٦/٧٦٦ مفتاحًا في اللغتين بلا مفتاحٍ ميت ولا مفقود. التوثيق في README لحق بالتغيير: سطرُ Vercel
وشرحُ التحجيم، وتصحيحُ عدَّيْن قديمين (روابط الـsitemap وفحوص الـAPI).

## [1.3.0] — 2026-09-05

### أُضيف

- **موقعٌ مستضاف من القالب** (`52d62c2` · `ceebee8`): `server/sites.js` يحفظ المواقع ويولّد صفحاتها
  بنفس `src/data/deliverable.js` التي تُسلَّم بها الحزمة — فلا نسخةَ ثانية من المنطق تُخطئ وحدها.
  حصّة التحرير عشرُ عمليات في الشهر للمجاني (مع شعارنا على الصفحة)، وبلا حدّ للمميزة (١٩ ريالًا
  شهريًا) مع النطاق الخاص و`/s/<slug>/print` بمقاس A4. `/host` يعرض المواقع و`/studio` يحرّرها
  برابط تحريرٍ من ثمانية عشر حرفًا لا كلمة سرّ: يُصدَّر من اللوحة، ولا يظهر في أيّ CSV إداري.
  الأرقام كلها تُقرأ من `PLANS`/`HOST_LIMITS`؛ وترقية الخطة يدُ موظفٍ لا زرٌّ في الشاشة.
- **فاحص ATS مجاني على `/ats`** (`892b5d4`): سبعةُ شروط تُقاس داخل المتصفح وحده — ملخّص، عنوان
  خبرة معروف، مهارات مستقلّة، تعليم، بريدٌ يُقرأ، تواريخ على نسقٍ ثابت، ورصاصاتٌ نصفُها برقم —
  مع طولٍ بين ٣٢٠ و٩٠٠ كلمة ونطاقِ نجاحٍ ٧٠ وأقلّ من ٤٠ كلمة لا تُقاس. الجدول الواحد
  `src/data/ats-table.js` يقرأه الفاحص وشارةُ القالب في المتجر و`scripts/check-ats.mjs` الذي
  يُرفَع داخل كل حزمة (١٥ حزمة). لا `fetch` ولا `localStorage` في مسار التحليل، وPDF مرفوضٌ
  بصراحة لا بقائمة أعذار.
- **تراخيص المقاعد للمعاهد على `/b2b`** (`42e7e81`): أربع باقات من `src/data/b2b.js` —
  ٥٠/١٥٠/٣٠٠/٥٠٠ مقعد مقابل ١٥٠٠٠/٢٤٠٠٠/٣٣٠٠٠/٤٥٠٠٠ ريالًا في السنة، وهي المعادلة نفسها
  لكل مقعد (٣٠٠/١٦٠/١١٠/٩٠ ريالًا). `server/orgs.js` يفتح `GET /org/:code` و`POST /org/redeem`
  و`/admin/orgs{.csv}`، والردود بأسبابٍ لا بحالات: `orgCode` · `email` · `name` · `template` ·
  `expires` · `paused` · `exhausted` · `already` · `unknown`. المقعد المستهلك يصبح طلبًا حقيقيًا
  (`total = 0`، `discount = 89`، مفتاح ترخيص) يفتح نفس رابط التسليم الموقّع — مُتحقَّقٌ منه على
  خادم فعلي: حزمة ٥٦٬١٢٢ بايتًا بواحدٍ وعشرين ملفًا، و`resume.html` مطبوعٌ فيه اسم الطالب.
  الرمز `QALB-XXXX-XXXX` من أبجديةٍ من ٣٢ حرفًا بلا ما يُخلط بالأرقام، ليُنطَق في مكتب القبول
  دون أن يُعاد كتابته. العقد سنةٌ واحدة بلا تجديدٍ تلقائي.
- **تقريرُ الاستهلاك عمودٌ لا وعد**: `orgUsage` في `src/data/b2b.js` يحسب ما استُهلك من كل قالب
  لحظة قراءة السجلّ، فلا يُخزَّن عن الطالب شيءٌ جديد؛ وصار `/admin/orgs.csv` بتسعة أعمدة آخرها
  `byTemplate` بصيغة `nova:4;mirrorbundle:1`، وصار العمود نفسه في جدول اللوحة. أربعُ فحوصٍ على
  خادم فعلي تثبّت أن الأرقام تُقرأ وأن لا بريد طالبٍ في الملف.

- **تبويب «المقاعد» في اللوحة** (`42e7e81`): العقود، إصدارُ رمز، إيقافٌ واستئناف، وتعديلُ
  المقاعد (لا تنزل تحت ما استُهلك) والأشهر، وتصديرُ العقود CSV بثمانية أعمدة فيها بريد الجهة
  وحده. ٢٨ مفتاحًا جديدًا للوحة في اللغتين.

### صُحّح

- **بوابة المقاعد لم تكن مفتوحة في التطوير** (`vite.config.js`): `PROXY_PATHS` كان ينقل
  `/orders` و`/admin/` و`/download` و`/dl/` ولا ينقل `/org`، فاستهلاكُ مقعد في
  `VITE_QALB_API=rest npm run dev` يرجع بـ`index.html` بدل JSON — أي نموذجٌ يبدو أنه يعمل ثم
  لا يفسّر شيئًا. أُضيف المسار إلى جسر `server` و`preview` جميعًا، وفحصٌ في `tests/smoke.mjs`
  يقرأ `vite.config.js` نفسها ويتأكد أن `/org` فيها. والتحقّق من الأصل المُعايَن لا من المصدر:
  `201` لاستبدال المقعد، وحزمة ٣٦٬٢٦٢ بايتًا بثلاثة عشر ملفًا باسم الطالبة، و`check-ats.mjs`
  داخلها، و`401` لقائمة العقود بلا جلسة.
- **شريطُ الباقات كان يَعِد بتقريرٍ لا يولّده أحد**: «تقرير استخدام في آخر السنة» و«صفحة استبدال
  خاصة بالفوج» كانا يُقرَآن كمنتَجَيْن آليَّين، ولا مُولِّد لهما في المستودع. صارت الباقات تقول
  الحقيقة: التقرير يُكتب يدويًا من دفتر الاستهلاك، وكل فوجٍ يأخذ رمزًا مستقلًّا على نموذج الاستبدال
  نفسه (`src/data/b2b.js`).

- **سطرٌ ناقص كان يبتلع الطلب التالي** (`42e7e81`): `save()` في `server/worker.js` كان يلحق
  الطلب بفاصلةٍ دون أن ينظر إلى آخر الملف؛ فإن انتهى `orders.jsonl` بسطرٍ بلا `\n` — ملفٌ كُتب
  يدويًا أو سطرٌ نُصِف — التصق الطلبُ الجديد به وألقاه `load()` بصمت، وهو أسوأ ما يقع لدفترٍ فيه
  مال. صار يقرأ بايتًا واحدًا ويضع الفاصل عند الحاجة. كشفته إضافةُ فحص المقاعد إلى
  `tests/admin-api.mjs`: الإيصال يعيد 404 ومفتاحُ الترخيص `valid:false`.
- **حرّاسٌ عميت عن `/b2b`** (`42e7e81`): اشتقاقُ الـsitemap من جدول المسارات كان
  `path="(\/[a-z-]*)"` فيتخطّى أي مسارٍ فيه رقم؛ صار `[a-z0-9-]` والعدد ٢٤ رابطًا.
- **الخنق كان يعاقب الجهة الأمينة** (`42e7e81`): كلُّ فشلٍ في `/org/redeem` كان يُحتسب تخمينًا،
  فغلطةُ نسخٍ في رمز تُغلق بوابة الدفعة كلها. الآن `unknown` وحده يُحتسَب — ثلاثٌ في الدقيقة
  فتُقفل بـ`429`.

### تغيّر

- اللوحة تقرأ باقات المقاعد من وحدة البيانات نفسها (`B2B_TIERS`/`B2B_TERM_MONTHS`) التي يقرأها
  `server/orgs.js`؛ لا رقمَ في اللوحة لا يُصدِّقه الخادم.
- قائمة قوالب العقد = ما له قيمة ATS **مقيَسة**، لا ما يرفع شارة «توافق ATS ٩٧+» في المتجر:
  لذلك يدخل `atlas-cv` (٩٢) في عقد المقاعد ولا يظهر تحت فلتر الشارة. الفرق مقصودٌ ومكتوبٌ في
  `src/data/b2b.js`، فلا يُ«يُصحَّح» لاحقًا بمحاذاة الرقمين.
- `index-*.js` صار ٢٧٥٫٢٥ ك.ب (gzip ٩٣٫٩٦): زيادته نصوص `b2b.*` في القاموس المشترك بين اللغتين؛
  أما صفحة المقاعد نفسها فـ`B2B-*.js` بحجم ١٤٫٣٧ ك.ب (gzip ٤٫٠٦) تُحمَّل معها وحدها.

### الاختبارات والبناء (مخرجات فعلية لا وصف)

```
node tests/smoke.mjs      → all 45 check groups passed · 29 routes / 151 expectations
                            b2b 47 · ats 52 · hosting 38 · personalisation 50 · delivery 17
node tests/admin-api.mjs  → 169 passed, 0 failed   (٣٧ منها للمقاعد على خادم فعلي)
npm run gen:seo           → seo: 24 urls in sitemap · robots.txt · og-cover.png=ok · public/og=15 بطاقات
npx eslint . · npx prettier --check .  → صفرَ سطر
```

القاموس: ٧٥٩ مفتاحًا للمتجر و١٧٤ للوحة، `ar` و`en` متطابقان ورقةً بورقة، ولا مفتاحَ ميتًا —
يثبّتها فحص `i18n`. التسليم للتوأم المجاني يُختبَر من جهة الخادم بإيصالٍ حقيقي وحزمةٍ موقّعة،
ولا يُختلق في الوضع المحلي: صفحة `/b2b` تُبدّل الاستمارة برسالةٍ إلى البريد الرسمي.

## [1.2.0] — 2026-09-04

### أُضيف

- **تسليم حقيقي لكل منتج** (`6e8842e`): ١٥ حزمة تُبنى من `src/data/deliverable.js` وحده،
  ورابط تنزيل موقَّع بـHMAC لكل طلب على حدة — صالح ١٠ دقائق، يُستعمل مرة واحدة، ومحفوظ
  في دفتر `server/downloads.jsonl`. لا ملف ثابت في `public/` بعد اليوم، ولا زرّ «تنزيل»
  يعرض نجاحًا بلا بايتات ورائه.
- **حقن بيانات المشتري في الملفات المسلَّمة** (`e500758`): اسم ومسمى وبريد وجوال ورابط
  ونبذة — اختيارية، من بطاقة واحدة في صفحة المنتج والدفع، تُنقَّى بدالة واحدة
  (`sanitizePersonal`) يستعملها المتجر والخادم، وتُطبع في `content/profile.json`
  و`resume.html` والترويسة والتذييل و`LICENSE.txt`. حقل فيه `< >` يُرفَض كلّه مع تنبيه
  على الحقل نفسه، لا نسخة مشوَّهة.
- **صفحة `/legal` بأقسام مرساة** (`55fb311`): الشروط، الخصوصية، الاسترجاع، الرخصة —
  مكتوبة كما ينفّذها الكود، بعد أن كانت أربعة روابط في التذييل تنزل على شريط الحقوق.
- **زرّ «امسح بياناتي من هذا المتصفح»** (`55fb311`): يفرّغ الحقول، يُلغي التحديد، يمحو
  `qalb.personalize.v1`، ويخبر المشتري بما يبقى في سجلّ الطلبات.
- **`server/seal.js`** (`436a792`): كل ملف حالة يُختم `0600` عند الإنشاء **وعند الإقلاع**،
  ومجلد `QALB_DATA_DIR` يُنشأ `0700`.
- **ورقة طباعة لصفحة `/legal`**: كتلة `@media print` في `src/index.css` تقلب بتّات
  `--c-*` إلى ورق أبيض وتُخفي الشريط والتذييل والأزرار والحقول والزخرفة، وتضع هوامش
  `@page`، وتطبع المسارات المذكورة في النص بجانبها (`/track`) — فالزرّ لا يُخرج شاشة داكنة على الورق.
- **حارسا نصّ في `npm test`**: لا سطر يعد بإرسال بريد أو بإصدار فاتورة بلا نفي في نفس
  السطر؛ ولا ادّعاء شهادة أو تشفير (`PCI-DSS` · `SOC 2` · `دفع مشفّر`…) في أي ملف من `src/`.
- **فحص المراسي**: كل `to="…#anchor"` يجب أن يقابل `id` موجودًا في ملف الصفحة التي يدلّ
  إليها المسار، تُقرأ خريطة المسارات من `src/App.jsx`.

### صُحّح

- `/legal` لم يكن في `public/sitemap.xml` (٢٠ رابطًا مكتوبة قائمةً في `scripts/seo.mjs`)؛ أُضيف،
  وصار الفحص يشتق القائمة من جدول المسارات في `src/App.jsx` بعدّ عددٍ ثابت، و`robots.txt` يُطالَب
  بإبعاد المسارات الخاصة الأربعة كلها. الفحص يقرأ `dist/assets/*.css` إن وُجد، لذا تُقدَّم `build`
  على `test` في CI ويصبح وعد الطباعة مختبَرًا على الملف المُسلَّم لا على المصدر وحده.

- الإيصال وسطر الدفع كانا يقولان إن الملفات والمفتاح «تُرسَل بالبريد»، و`invoiceNote`
  يقول إن فاتورة ZATCA إلكترونية «تُصدَر تلقائيًا» (`a9ddbf4`) — ولا مُرسِل ولا تكامل
  ضريبي في المستودع. الصياغة الآن تصف ما يحدث، في اللغتين.
- دفاتر البيانات كانت `0644` على القرص (`436a792`): `orders.jsonl` (اسم المشتري وجواله
  ونبذته)، `downloads.jsonl` (عنوان IP)، `admins.json`، ومفتاحا التوقيع. سطر تالف واحد
  في دفتر الطلبات كان يُسقط `load()` كله؛ الآن يُتجاوَز ويُذكر برقمه في سجلّ الخادم.
- `credential.helper store` مع `HOME` داخل جذر المستودع كان يُسقط `/.git-credentials` و`/.gitconfig`
  داخل شجرة العمل نفسها — أي أن توكن الوصول على بُعد `git add -A` واحد من أن يُنشر في المستودع الذي
  يصادق عليه. `​.gitignore` يمنعها الآن (مع `/qalb-*.bundle`)، ومخزن الاعتمادات المستعمل هنا صار
  خارج الشجرة (`/tmp/qalb-cred`، وضع 0600) بوصل `-c` لكل أمر بلا أي إعداد عام، والفحص قبل كل دفع
  يبحث عن `ghp_`/`github_pat_` في `main` ويوقف الدفع إن وجد.
- **شارة PCI-DSS وسطر «SSL · دفع مشفّر وآمن»** استُبدلا بجملة واحدة صادقة
  `cart.secure` تُستعمل في أربعة مواضع: الدفع محاكى، لا بيانات بطاقة تُحفَظ، لا مبلغ يُخصَم.
- قراءة كتالوج اللوحة صارت آمنة مع صفوف ناقصة، و«الوحدات القديمة» بعد نشر تُعاد تحميلها
  مرة واحدة لا تتكرر (`4a70d04`).

### تغيّر

- `maxLength` في حقول التخصيص صار سقف المنقِّي نفسه (`PERSONAL_LIMITS`) بدل ١٦٠ للجميع،
  فلا بتر صامت بعد آخر حرف.
- روابط الشروط/الاسترجاع/الخصوصية في خطوة الدفع خارج `<label>` (داخله كان النقر يقلّب
  مربع الموافقة) وتُفتح في تبويب جديد حتى لا يضيع ما كتبه المشتري.
- لوحة الإدارة تعرض علم «مُخصَّص ببيانات المشتري» على صف الطلب، وملف CSV يلحق العمود
  نفسه — بدون نسخ الاسم أو الجوال أو النبذة إلى التصدير.

### الاختبارات والبناء (مخرجات فعلية لا وصف)

- `node tests/smoke.mjs` → **٣٤ مجموعة · ٢٢ مسارًا · ١١١ توقّعًا** (التخصيص ٥٠، التسليم ١٧،
  قمع الدفع ١٣، i18n ١١).
- `node tests/admin-api.mjs` → **٧٩ نجح · ٠ فشل** (من ٧٢ قبل الجولة).
- `npx eslint .` و`npx prettier --check .` صامتان؛ `npm run build`: `index-CKAUl97f.js` ‏229.65 kB
  (78.23 kB gzip)، `Admin-Cr31vg_z.js` ‏47.49 kB (14.16 kB)، `Legal-rNkdZ3Sv.js` وحدة مستقلة 3.31 kB (1.17 kB)، `index-CY2vQks8.css` ‏92.27 kB (14.39 kB gzip).
- قاموس المتجر ٤٦٤ مفتاحًا لكل لغة (كان ٤١٦)، ولوحة الإدارة ١٤٦.

## [1.1.0] — 2026-09-03

- **لوحة إدارة كاملة** (`5f56740`): منتجات ودخل وصلاحيات؛ `admin.js` بلا اعتماديات —
  scrypt + كوكي جلسة + خنق محاولات + حارس CSRF، وتعديلات الكتالوج طبقة تجاوزات لا تعيد
  كتابة `src/data/templates.js`.
- إيراد صادق الإسناد، وdev proxy لوضع `rest`، ومجلد بيانات منفصل (`080adb0`).
- فحص واجهة يصمد أمام API غير reachable (`4549200`)، وأسماء خطوات CI تذكر الحزمتين (`90529e0`).
- **إصلاح ظاهري حاسِم**: تسميات الأزرار والحقول كانت غير مرئية لأن إعادة الضبط المكتوبة باليد كانت
  خارج `@layer` فتغلب الـutilities — صُحّح وأُضيف فحص يقرأ stylesheet المبنى لا jsdom وحده (`5af6ccc`).

## [1.0.0] — 2026-09-03

أول إصدار عام: متجر قوالب (مواقع + سير ذاتية + حزمتان) بواجهة عربية RTL وتبديل حيّ إلى
الإنجليزية، معاينات مرسومة بالكود لا صور جاهزة، سلة وكوبونات وVAT ١٥٪ ودفع ثلاثي الخطوات
بمفتاح ترخيص، صفحات `/track` و`/licence` وحدود خطأ لكل مسار، بطاقات مشاركة لكل منتج،
خطوط مستضافة محليًا مع نصوص تراخيصها، و`qalb@qalb.store` بريدًا رسميًا واحدًا.
