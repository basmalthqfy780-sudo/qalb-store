node_modules
dist
tests/build
coverage
uploads/
.npm/

# بيانات حيّة يكتبها خادم الطلبات — لا تُودَع أبدًا: دفاترُ الطلبات والمؤسسات
# والمفاتيحُ والحسابات. النمطُ يشمل كلَّ ما يولّده الخادم لاحقًا، فلا يُنسى دفترٌ.
server/*.json
server/*.jsonl
server/.admin-secret
server/.download-secret

# قد يكتب المشغّل credential.helper store داخل جذر المستودع (~ = مجلد العمل):
.git-credentials
.git-credentials.lock
!.git-credentials.example

# HOME هنا هو جذر المستودع: أي إعداد عام أو حزمة مؤقتة يسقطان داخل الشجرة
/.ssh/
/.gitconfig
/qalb-*.bundle
# حزم التسليم المولّدة للاستضافة اليدوية — ليست جزءًا من المستودع
dist-deliverables/

.cache
.DS_Store
*.local
.env
.env.*
!.env.example
